# API
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
python-dotenv>=1.0
pydantic>=2.7

# Database / storage
supabase>=2.5

# ML — dataset pipeline + inference
ultralytics>=8.2
albumentations>=1.4
opencv-python-headless>=4.10
pillow>=10.3
numpy>=1.26

# Zero-shot "is this even raw meat?" gate before the fresh/spoiled
# classifier runs — see app/services/meat_gate.py. Pretrained, no training
# of our own needed.
open_clip_torch>=2.24
